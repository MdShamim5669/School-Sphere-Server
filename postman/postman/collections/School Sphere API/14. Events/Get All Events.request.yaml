$kind: http-request
url: "{{baseUrl}}/events?page=1&limit=10"
method: GET
queryParams:
  - key: page
    value: "1"
  - key: limit
    value: "10"
  - key: classId
    value: "{{classId}}"
    disabled: true
order: 2000
