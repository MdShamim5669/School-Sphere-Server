$kind: http-request
url: "{{baseUrl}}/attendances/{{attendanceId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "present": false
    }
order: 3000
