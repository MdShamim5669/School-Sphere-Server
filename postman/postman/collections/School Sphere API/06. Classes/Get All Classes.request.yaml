$kind: http-request
url: "{{baseUrl}}/classes?page=1&limit=10"
method: GET
queryParams:
  - key: page
    value: "1"
  - key: limit
    value: "10"
  - key: gradeId
    value: "{{gradeId}}"
    disabled: true
  - key: supervisorId
    value: "{{teacherId}}"
    disabled: true
order: 2000
