$kind: http-request
url: "{{baseUrl}}/announcements/{{announcementId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "title": "Important School Holiday Notice"
    }
order: 3000
